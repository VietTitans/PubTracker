server {
    listen 80;
    server_name _;
    root /usr/share/nginx/html;
    index index.html;

    # SPA fallback - serves index.html for /callback (and any other client route) since
    # there's no matching static file for it.
    location / {
        try_files $uri /index.html;
    }
}
